# 🔫 CS:RPG: Quantum Breach
## 技术设计文档 v1.0

---

# 第一部分：项目概览

## 1.1 项目信息

| 项目 | 内容 |
|------|------|
| **项目名称** | CS:RPG: Quantum Breach |
| **类型** | FPS / RPG / 动作冒险 |
| **平台** | WebGL (浏览器) + 移动端适配 |
| **目标帧率** | PC 60fps / 移动端 30fps |
| **目标时长** | 6-8小时游戏内容 |
| **美术风格** | 赛博朋克写实 (Cyber-Realistic) |

---

# 第二部分：技术架构

## 2.1 技术栈

- **渲染**: Three.js + @react-three/fiber
- **物理**: Cannon-es
- **状态管理**: Zustand
- **构建**: Vite + TypeScript
- **UI**: React + CSS Modules

## 2.2 核心系统模块

```
┌─────────────────────────────────────────────────────────────┐
│                        技术架构图                              │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ FPS核心  │  │ RPG系统  │  │  AI系统  │  │ 叙事引擎 │  │
│  │移动/射击 │  │等级/技能 │  │行为树/Boss│  │剧本/对话 │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ 关卡系统  │  │ 渲染引擎 │  │ 移动端   │                 │
│  │ 地图/机关│  │ WebGL   │  │ 触控优化 │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

---

# 第三部分：核心系统详细设计

## 3.1 FPS 射击系统

### 3.1.1 武器数据接口

```typescript
interface WeaponData {
  id: string;
  name: string;
  type: WeaponType;
  category: 'primary' | 'secondary' | 'melee' | 'equipment';
  
  // 伤害属性
  damage: number;
  damageFalloff: FalloffCurve;
  damageType: DamageType;
  
  // 射程
  effectiveRange: number;
  maxRange: number;
  
  // 射速 (RPM)
  fireRate: number;
  fireMode: FireMode;
  burstCount: number;
  
  // 弹药
  magSize: number;
  totalAmmo: number;
  reloadTime: number;
  reloadType: ReloadType;
  
  // 后坐力
  verticalRecoil: number[];
  horizontalRecoil: number[];
  recoilRecovery: number;
  
  // 散布
  hipSpread: SpreadConfig;
  adsSpread: SpreadConfig;
  
  // 音效
  shootSound: string;
  reloadSound: string;
  
  // 特效
  muzzleFlash: string;
  tracerColor: string;
}

type WeaponType = 'rifle' | 'pistol' | 'shotgun' | 'sniper' | 'smg' | 'lmg' | 'grenade' | 'melee';
type FireMode = 'semi' | 'auto' | 'burst';
type DamageType = 'physical' | 'energy' | 'armor_piercing' | 'explosive';
```

### 3.1.2 武器列表

| 武器 | 类型 | 伤害 | 射速(RPM) | 特点 |
|------|------|------|-----------|------|
| M4A1 | 步枪 | 33 | 800 | 均衡 |
| AK-47 | 步枪 | 38 | 600 | 高伤害，高后坐力 |
| AWP | 狙击 | 115 | 40 | 一击必杀 |
| MP5 | 冲锋 | 26 | 950 | 低后坐力 |
| USP-S | 手枪 | 35 | 350 | 消音 |
| Desert Eagle | 手枪 | 62 | 300 | 高伤害 |
| SPAS-12 | 霰弹 | 120×8 | 120 | 高伤近战 |
| M249 | 机枪 | 35 | 800 | 大弹链 |

### 3.1.3 后坐力系统

```typescript
class RecoilSystem {
  // 每一发应用后坐力到视角
  applyRecoil(camera: Camera, weapon: Weapon): void {
    const vRecoil = weapon.data.verticalRecoil[this.shotIndex % 8];
    const hRecoil = weapon.data.horizontalRecoil[this.shotIndex % 8];
    
    camera.rotation.x += vRecoil * 0.1; // 垂直
    camera.rotation.y += hRecoil * 0.05; // 水平
  }
  
  // 每帧恢复
  recover(camera: Camera, delta: number): void {
    camera.rotation.x = lerp(camera.rotation.x, 0, delta * 5);
    camera.rotation.y = lerp(camera.rotation.y, 0, delta * 5);
  }
}
```

---

## 3.2 RPG 成长系统

### 3.2.1 玩家属性

```typescript
interface PlayerStats {
  // 基础
  level: number;
  experience: number;
  
  // 战斗
  health: number;
  maxHealth: number;
  armor: number;
  maxArmor: number;
  
  // 移动
  speed: number;
  sprintSpeed: number;
  
  // 武器加成
  weaponDamageBonus: number;
  fireRateBonus: number;
  reloadSpeedBonus: number;
  spreadReduction: number;
  recoilControl: number;
  
  // 防御
  damageReduction: number;
  criticalChance: number;
  criticalDamage: number;
}
```

### 3.2.2 经验曲线

```typescript
// 经验需求公式
function getExpForLevel(level: number): number {
  return Math.floor(100 * Math.pow(1.5, level - 1));
}

const MAX_LEVEL = 50;
```

### 3.2.3 技能树结构

```
战斗专精 (Combat)
├── 枪械专精 +10%武器伤害
├── 快速瞄准 -20%瞄准时间
├── 暴击强化 +5%暴击率
├── 穿透弹 +15%穿甲
└── 火力全开 终极: 100%射速提升

生存 (Survival)
├── 急救包 +1急救包槽位
├── 护甲强化 +20%护甲
├── 闪避 +10%闪避
├── 意志坚定 -15%减速效果
└── 快速恢复 +50%回血速度

特殊 (Special)
├── 手雷大师 +2手雷
├── 战术扫描 显示敌人
├── 隐形 终极: 5秒隐形
└── EMP冲击 范围沉默
```

### 3.2.4 装备稀有度

| 稀有度 | 颜色 | 掉落率 | 特效槽 |
|--------|------|--------|--------|
| Common | 灰色 | 60% | 0 |
| Uncommon | 绿色 | 25% | 1 |
| Rare | 蓝色 | 10% | 2 |
| Epic | 紫色 | 4% | 3 |
| Legendary | 橙色 | 1% | 4 |

---

## 3.3 AI 系统

### 3.3.1 行为树

```typescript
// 敌人AI行为树
const enemyBehavior = {
  root: 'selector',
  children: [
    {
      type: 'sequence',
      children: [
        { type: 'condition', test: 'isHealthLow' },
        { type: 'action', behavior: 'retreat' }
      ]
    },
    {
      type: 'sequence',
      children: [
        { type: 'condition', test: 'hasLineOfSight' },
        { type: 'action', behavior: 'attack' }
      ]
    },
    {
      type: 'action', behavior: 'patrol'
    }
  ]
};
```

### 3.3.2 敌人类型

| 敌人 | 生命 | 护甲 | 伤害 | 特点 |
|------|------|------|------|------|
| 变异体 | 50 | 0 | 10 | 群居 |
| 精英士兵 | 120 | 30 | 20 | 会找掩体 |
| 重装兵 | 300 | 100 | 35 | 护盾 |
| 自爆兵 | 80 | 0 | 80 | 近战自爆 |

---

## 3.4 Boss 系统

### 3.4.1 Boss 列表

| Boss | 章节 | 阶段 | 特殊机制 |
|------|------|------|----------|
| 训练官 X-1 | Ch.1 | 2 | 教学 -> 实战 |
| 巨型机械臂 | Ch.2 | 3 | 抓取/激光/锤击 |
| 变异列车长 | Ch.3 | 3 | 召唤/冲刺/毒气 |
| AI博士 | Ch.4 | 3 | 激光/无人机 |
| 组合金刚 | Ch.5 | 4 | 形态切换 |
| 虚空吞噬者 | Ch.6 | 5 | 传送/虚空炮 |

### 3.4.2 Boss阶段机制示例

```typescript
// 训练官 Boss
const bossTrainer = {
  phases: [
    {
      name: '训练模式',
      health: 500,
      behavior: 'teaching',
      mechanics: ['demonstration', 'duel_invite']
    },
    {
      name: '实战模式',
      health: 500,
      behavior: 'aggressive',
      mechanics: ['combo', 'flashbang', 'enrage']
    }
  ],
  enrage: { trigger: 'hp_20%', effects: { damage: 1.5, speed: 1.3 } }
};
```

---

## 3.5 叙事系统

### 3.5.1 主线剧情大纲

```
Chapter 1: 训练营
- 序章: 量子裂隙出现
- 训练官X-1教你战斗
- Boss战: 击败训练官获得认可

Chapter 2: 废弃工厂
- 探索被遗弃的工业区
- 遭遇变异体群
- Boss战: 巨型机械臂

Chapter 3: 地铁站
- 深入地下铁路
- 列车长变异成怪物
- Boss战: 变异列车长

Chapter 4: 研究所
- 进入秘密实验室
- 揭露AI叛变真相
- Boss战: AI博士

Chapter 5: 城市废墟
- 返回地面，已成废土
- 发现巨型机甲
- Boss战: 组合金刚

Chapter 6: 裂隙核心
- 前往裂隙源头
- 最终决战
- Boss战: 虚空吞噬者
```

### 3.5.2 对话数据结构

```typescript
interface Dialogue {
  id: string;
  speaker: string;
  text: string;
  choices?: { text: string; next: string; condition?: Condition }[];
  onComplete?: Action[];
}
```

---

## 3.6 关卡设计

### 3.6.1 关卡结构

```typescript
interface Level {
  id: string;
  name: string;
  duration: number; // 分钟
  
  // 场景
  environment: EnvironmentConfig;
  
  // 敌人配置
  waves: Wave[];
  boss?: BossSpawn;
  
  // 目标
  objectives: Objective[];
  
  // 检查点
  checkpoints: Checkpoint[];
}
```

### 3.6.2 章节时长

| 章节 | 名称 | 预计时长 | 敌人波次 |
|------|------|----------|----------|
| Ch.1 | 训练营 | 10min | 3波 |
| Ch.2 | 废弃工厂 | 12min | 4波 |
| Ch.3 | 地铁站 | 12min | 4波 |
| Ch.4 | 研究所 | 15min | 5波 |
| Ch.5 | 城市废墟 | 15min | 5波 |
| Ch.6 | 裂隙核心 | 20min | 6波 |

---

## 3.7 移动端适配

### 3.7.1 触控方案

```
┌────────────────────────────────────┐
│                                    │
│              游戏画面               │
│                                    │
│                                    │
│  ┌────┐                    ┌────┐  │
│  │ 摇 │                    │ 开 │  │
│  │ 杆 │     ┌──────┐       │ 火 │  │
│  └────┘     │ 准星 │       └────┘  │
│             └──────┘               │
│  ┌────┐              ┌────┐       │
│  │ 换 │              │ 跳 │       │
│  │ 弹 │              │ 跃 │       │
│  └────┘              └────┘       │
└────────────────────────────────────┘
```

### 3.7.2 自适应UI

- 虚拟摇杆: 左侧半屏
- 射击按钮: 右侧
- 瞄准: 射击按钮长按
- 跳跃: 右侧按钮

---

# 第四部分：文件结构

```
cs-rpg/
├── src/
│   ├── core/              # 核心引擎
│   │   ├── Game.ts
│   │   ├── GameLoop.ts
│   │   ├── InputManager.ts
│   │   ├── EventBus.ts
│   │   └── StateMachine.ts
│   │
│   ├── engine/
│   │   ├── renderer/     # 渲染
│   │   ├── physics/      # 物理
│   │   ├── audio/        # 音频
│   │   └── animation/    # 动画
│   │
│   ├── systems/
│   │   ├── fps/          # FPS系统
│   │   ├── rpg/          # RPG系统
│   │   ├── ai/           # AI系统
│   │   ├── story/        # 叙事
│   │   └── level/        # 关卡
│   │
│   ├── data/             # JSON配置
│   │   ├── weapons.json
│   │   ├── enemies.json
│   │   ├── bosses.json
│   │   ├── skills.json
│   │   ├── levels.json
│   │   └── story.json
│   │
│   ├── ui/              # UI组件
│   │   ├── HUD/
│   │   ├── Menu/
│   │   └── Dialog/
│   │
│   ├── mobile/          # 移动端
│   │   ├── TouchControls.ts
│   │   └── VirtualJoystick.ts
│   │
│   └── main.ts
│
├── public/assets/
│   ├── models/
│   ├── textures/
│   └── audio/
│
├── package.json
├── tsconfig.json
├── vite.config.ts
└── index.html
```

---

# 第五部分：子代理分配

## 5个Sub-Agent任务分配

| Agent | 任务 | 交付物 |
|-------|------|--------|
| **Agent-1** | FPS核心系统 | `src/systems/fps/` 武器/移动/物理 |
| **Agent-2** | RPG系统 | `src/systems/rpg/` 等级/技能/装备 |
| **Agent-3** | AI与Boss | `src/systems/ai/` 行为树/Boss |
| **Agent-4** | 叙事与关卡 | `src/systems/story/` + `src/data/` |
| **Agent-5** | 渲染与移动端 | `src/engine/` + `src/mobile/` |

---

# 第六部分：开发优先级

## Phase 1: 核心玩法
1. 玩家移动 + 跳跃
2. 武器射击 + 后坐力
3. 敌人AI基础
4. 第一章关卡

## Phase 2: RPG内容
1. 等级系统
2. 技能树
3. 装备掉落
4. Boss战斗

## Phase 3: 完善
1. 叙事对白
2. 后续章节
3. 移动端优化
4. 渲染优化
